# Expected Workspaces

Run `render_task_bundle.py` against an example task contract to create a complete task workspace. This directory documents the expected shape without storing generated task-specific artifacts in the workflow repo.
