# Claude Builder Prompt

Use the corresponding root prompt as the canonical wording: `prompts/claude-builder-prompt.md`.

Humanize adapters should write durable artifacts under `.humanize/`, `review_rounds/`, `review_issues.jsonl`, `review_waivers.jsonl`, and `evidence/`.
